"""Offline analysis of participant data and trained policies.

Reads immutable snapshots only — never the live deployment, never the rotating
`data/replays/` buffer. See docs/completion/05-study-data.md.
"""
