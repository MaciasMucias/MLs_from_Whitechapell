# Final runs — 04-final-runs.md. 9 tasks, submit with --array=0-8%9
#
# PREREQUISITES
#   01        cops frozen at COPS_STUDY_V2
#   02-C1     reward coefficients exposed as CLI flags (the sparse arm needs them)
#   02-C2     best-checkpoint tracking + pruning (9 concurrent runs x ~1.1GB otherwise)
#   03        <LR> and <ENT> below replaced with the sweep's chosen values
#
# INVARIANTS
#   - identical --n-envs/--n-steps across every line AND the sweep (batch = n_steps * n_envs)
#   - the same three seeds in every arm, so the comparison is paired per seed
#   - --wandb-run doubles as the checkpoint subdirectory name (train.py:291)
#
# Director ON — the headline arm.
# ADD THE DIRECTOR FLAGS CHOSEN BY 09 to all three lines (--initial-difficulty,
# --curriculum-kp, --curriculum-max-step, --curriculum-ratchet, band). Leaving
# them at defaults reruns the one configuration that already lost to the OFF arm
# on a fair Director-free evaluation. See 09-director-tuning.md.
#
# Rank these against the OFF arm on eval/win_rate ONLY. charts/win_rate on an ON
# run is the P-controller's setpoint (~0.5 by construction), not a score.
uv run python -m training.train --total-steps 15000000 --n-envs 12 --n-workers 12 --lr <LR> --ent-coef <ENT> --seed 27 --wandb-run director-on-s27
uv run python -m training.train --total-steps 15000000 --n-envs 12 --n-workers 12 --lr <LR> --ent-coef <ENT> --seed 28 --wandb-run director-on-s28
uv run python -m training.train --total-steps 15000000 --n-envs 12 --n-workers 12 --lr <LR> --ent-coef <ENT> --seed 29 --wandb-run director-on-s29
# Director OFF — the paired comparison arm. Identical but for --no-curriculum.
uv run python -m training.train --total-steps 15000000 --n-envs 12 --n-workers 12 --lr <LR> --ent-coef <ENT> --seed 27 --no-curriculum --wandb-run director-off-s27
uv run python -m training.train --total-steps 15000000 --n-envs 12 --n-workers 12 --lr <LR> --ent-coef <ENT> --seed 28 --no-curriculum --wandb-run director-off-s28
uv run python -m training.train --total-steps 15000000 --n-envs 12 --n-workers 12 --lr <LR> --ent-coef <ENT> --seed 29 --no-curriculum --wandb-run director-off-s29
# Sparse reward ablation (DESIGN_REQUIREMENTS 7.3) — terminal reward only, curriculum ON.
# The flags are --reward-*, NOT --alpha/--beta/...: a bare "--gamma 0" would set
# PPO's discount factor to zero instead of disabling the terminal bonus, and
# would do so silently. See 02-C1.
uv run python -m training.train --total-steps 15000000 --n-envs 12 --n-workers 12 --lr <LR> --ent-coef <ENT> --seed 27 --reward-alpha 0 --reward-beta 0 --reward-delta 0 --reward-gamma 0 --reward-zeta 0 --wandb-run sparse-s27
uv run python -m training.train --total-steps 15000000 --n-envs 12 --n-workers 12 --lr <LR> --ent-coef <ENT> --seed 28 --reward-alpha 0 --reward-beta 0 --reward-delta 0 --reward-gamma 0 --reward-zeta 0 --wandb-run sparse-s28
uv run python -m training.train --total-steps 15000000 --n-envs 12 --n-workers 12 --lr <LR> --ent-coef <ENT> --seed 29 --reward-alpha 0 --reward-beta 0 --reward-delta 0 --reward-gamma 0 --reward-zeta 0 --wandb-run sparse-s29
